import createServer from '../config/server';
const app = createServer();

describe('/data', () => {
  beforeAll(async () => {});
  afterAll(async () => {});

  describe('[POST]', () => {});
  describe('[GET]', () => {});
});
